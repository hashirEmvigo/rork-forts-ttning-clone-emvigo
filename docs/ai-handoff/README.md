# AI Handoff Location

The active AI handoff files have been moved into the application project folder:

```txt
web-cleanops/docs/ai-handoff/
```

Use that folder for all RORK wave instructions, ticket registry, outbox reports, and verification reports.

This root-level folder exists only as a pointer to avoid confusion.
