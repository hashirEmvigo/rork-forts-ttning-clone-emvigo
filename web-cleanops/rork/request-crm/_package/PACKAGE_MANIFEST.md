# Package Manifest

## Root

- `README.md` - package overview and read order.
- `VERSION.md` / `VERSION.json` - version metadata and hard implementation gates.
- `PACKAGE_MANIFEST.md` - this manifest.
- `CHECKSUMS.sha256` - generated checksums for package contents.

## Source

- `source/REQUEST_CRM_Arkitektur_RORK_v0.1_source.docx` - original uploaded source document.
- `source/REQUEST_CRM_Arkitektur_RORK_v0.1_extracted.txt` - extracted plain text source reference.

## Architecture docs

Located in `docs/architecture/request-crm/`.

## ADRs

Located in `docs/adr/`.

## RORK prompts and gates

Located in `rork/`.

## Schemas

Located in `schemas/`.

## Mock data

Located in `mock-data/`.

## UI specs

Located in `docs/ui/`.

## GitHub helpers

Located in `.github/` and `github/`.
