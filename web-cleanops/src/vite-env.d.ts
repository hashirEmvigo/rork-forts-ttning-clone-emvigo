/// <reference types="vite/client" />

/** Build-time marker baked by Vite `define` (see vite.config.ts). */
declare const __BUILD_TIMESTAMP__: string;
/** Build mode baked by Vite `define` ("production" | "development" | etc.). */
declare const __BUILD_MODE__: string;
