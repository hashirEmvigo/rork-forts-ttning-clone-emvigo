# AI Handoff Connection Test

> Repository migration note: the active working/master repository is now **`RiosBioz/Keymaster`**. The `RiosBioz/Stadportalen` reference below is **historical** — that repo is archive/frozen. Reason: Rork cannot connect an existing project to an existing GitHub repository (reconnecting creates a new repo). Future work uses `RiosBioz/Keymaster`.

Status: OK

Purpose:
This file confirms that ChatGPT can write to the Stadportalen repository through the GitHub connection.

Repository:
RiosBioz/Stadportalen

Created by:
ChatGPT GitHub connector
